# Commits

No `paths:` on purpose: it applies to every commit, so it loads in every session.

- Never add a `Co-Authored-By:` trailer, or any other agent attribution, to a commit
  message — whoever runs the agent is the only author.
